# Study Guide

A Next.js academic dashboard MVP for tracking courses, assessment weights, actual grades, expected grades, weighted averages and target-grade planning.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.

Data is saved in the browser with localStorage. The current version does not include authentication or cloud persistence.
